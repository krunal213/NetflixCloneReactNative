import { container } from "tsyringe";
import { SplashUseCase } from "./domain/splash/SplashUseCase"

/*container.register("AxiosInstance", {
  useValue: api,
});

container.register("AsyncStorage", {
  useValue: AsyncStorage,
});

container.register<NetworkDataSource>("NetworkDataSource", {
  useClass: NetworkDataSourceImpl,
});

container.register<LocalDataSource>("LocalDataSource", {
  useClass: LocalDataSourceImpl,
});

container.register<Repository>("Repository", {
  useClass: RepositoryImpl,
});*/





